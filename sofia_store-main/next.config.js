/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  swcMinify: true,
  // Remove or comment out any "output" or "exportPathMap"
  // output: "export",  <-- REMOVE THIS
};

module.exports = nextConfig;
